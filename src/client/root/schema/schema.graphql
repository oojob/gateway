scalar Date

type Edge {
	cursor: String!
	node: [Result!]!
}
type PageInfo {
	endCursor: String!
	hasNextPage: Boolean!
}

interface INode {
	id: ID!
	createdAt: Timestamp!
	updatedAt: Timestamp!
}

union Result = Job | Company

type Query {
	dummy: String!
}
type Mutation {
	dummy: String!
}
type Subscription {
	dummy: String!
}
schema {
	query: Query
	mutation: Mutation
	subscription: Subscription
}
